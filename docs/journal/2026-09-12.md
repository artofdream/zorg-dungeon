# 2026-09-12 — Difficulté level generator

- **Started from** `main` @ `11032fd` (knowledge companion refresh PR #20).
- **Producer does not merge (S7)** — PR opened for sponsor review.

## Shipped

- **`generateLevel` in `@zorg/engine`**: seed-stable, parseable DSL, legal
  Maker line layout ([[FR-5]]–[[FR-8]]), intended [[FR-46]] solvable within
  [[NFR-4]] caps. Evidence: `packages/engine/src/generator.ts`,
  `packages/engine/src/generator.test.ts`. See [[0005-level-generator]].
- Difficulté model is the authored numeric bands **1–4**, with knobs checked
  against playable fixture envelopes (not a 1–10 fantasy scale). Harder bands
  add rooms / a second hero / spells / E+O flavour. Band 4 matches the only
  authored Difficulté 4 board (9 rooms). Band 3 does not jump the board size
  vs band 2 — that matches the corpus.
- Construction guarantee: A, then a lethal `D`, then flavour rooms, then Z.
  First D damage ≥ every hero's HP. No mirrors (no intentional not_solvable
  worlds in this slice).
- **Does not invent:** `C` ([[FR-18]] / S2), Gunner duration (S3 — Gunner is
  not generated), [[FR-4]] earn/spend (S6), contracts 11–15 (S4), quarantine
  N11/N18 (CF-005). Mechanic / Princess / P / T skipped so the corridor proof
  stays a construction, not a hope.
- **Web:** campaign browser stays the authored default. A Generate panel
  (Difficulté 1–4) and Maker **Regenerate** are additive. Honesty copy:
  generated = Simulated engine, not Live & Probed; FR-4 still not gating.
- **Docs:** [[0005-level-generator]], this journal, [[PLAYER_GUIDE]] note.
  [[STATUS_LEDGER]] rows are **not** promoted. [[FINDINGS]] unchanged — no new
  CF. [[GAME_SPEC]] FRs not rewritten.

## Assumptions a reviewer can check

- Omitted `seed` is `1`, not `Date.now()`.
- Suggested layout is orientation 0, A-first / Z-last, default 4-hatch tiles.
- `checkSolvability` on that layout is Simulated proof, not a live probe.
- Generated campaign cards have `pack: "generated"` and no contract cost.

See [[GAME_SPEC]], [[STATUS_LEDGER]], [[PLAYER_GUIDE]], and [[0005-level-generator]].

## Ops — CF-006 closed

- [[FINDINGS]] CF-006 (production web image had no CD and went stale) is **Resolved**.
- CD privilege fix: [PR #19](https://github.com/artofdream/zorg-dungeon/pull/19) (`sudo git` on Lightsail `/opt/zorg`).
- Post-merge evidence: `deploy-web` on the [PR #21](https://github.com/artofdream/zorg-dungeon/pull/21) merge to `main` (`4111d82`) succeeded — Actions run [34687483872](https://github.com/artofdream/zorg-dungeon/actions/runs/34687483872).
- **Ops only.** Green CD is not a production content probe — [[STATUS_LEDGER]] FR/NFR rows are unchanged.

See [[FINDINGS]] CF-006 and [[0004-deployment-and-observability]].

## Honesty — [[FR-19]] / [[NFR-1]] / [[NFR-10]] Simulated

- **Started from** `main` @ `f625437` (CF-006 resolve PR #22).
- **Producer does not merge (S7)** — PR opened for coordinator review.

### Shipped

- Investigated Warrior / Elf / Mechanic / Gunner / Princess / pathing / scheduler: no `Math.random`, no wall-clock seed, no outcome-affecting unordered object iteration. Sorts used in choosers are key-stable. Generator `mulberry32` is authoring-only and is not a hero agent.
- **[[FR-19]] / [[NFR-1]]** replay evidence: same seedless layout + heroes + `castScript` → identical fingerprints. Covers Warrior, Elf, Mechanic, Gunner (2-arg; duration unused per S3), Princess, mixed pull, and Attack-after-spawn. Room-array shuffle does not change Warrior first-step. Evidence: `packages/engine/src/determinism.test.ts`.
- **[[NFR-10]]** per-layer chooser tests, isolated from `simulate()`: Warrior shortest + tie; Elf HP / length / tie / immunity; Mechanic shortest / shove / unjustified / power-tie / Mechanic tie order; Gunner fire-soonest / shot-factored path / 0-shot Warrior tie on a real 2-path square; Princess weight target / HP among equals / tie. Evidence: `packages/engine/src/hero-layers.test.ts`.
- Ledger only: [[FR-19]], [[NFR-1]], [[NFR-10]] → `Simulated`. No [[FR-4]] / [[FR-18]] / [[FR-43]] / [[FR-44]] / Gunner duration / contracts 11–15.
- [[FINDINGS]] CF-008: Mechanic shove search split `A:0:x,y` on the first
  `:`, so post-shove planning used the pre-shove graph. Codec is now `A:0@x,y`.
  Sensor: `packages/engine/src/mechanic.test.ts`. Recurrence 1.

See [[GAME_SPEC]], [[STATUS_LEDGER]], and [[0002-typescript-monorepo-2d-to-3d]].

## FR-43 / FR-44 — grounded win/loss and bonuses (this PR)

Started from `main` @ `f625437`; rebased onto `86664b4` (PR #23 honesty slice).
Producer does not merge (S7).

- **[[FR-43]]** `scoreLevel` aggregates after [[FR-45]] sequential resolve:
  loss on any world's Z entry; win only if every hero across every world is
  dead **and** every blocking constraint holds. Evidence:
  `packages/engine/src/constraints.test.ts`.
- **[[FR-44]]** blocking `constraints` vs separately-tracked `bonuses`
  (variants stay non-blocking). A failed or unsupported bonus does not
  prevent a win.
- Evaluators are only those grounded in [[GAME_SPEC]] + existing sensors:
  named-world solvability via [[FR-46]] (NFR-4 `budget` is unsupported, not
  a proof), `dist` / exists forms via [[FR-10]], HP floor, gold-at-death,
  reverse death order. Opaque authored prose (`Le donjon doit être carré`,
  spell-usage, distinct death rooms, immunity counterfactuals, setup
  “Remplacer…”) is `UnsupportedConstraint` — a blocking unsupported line
  refuses a win claim. See [[0006-grounded-constraint-evaluation]].
- Ledger: [[FR-43]] / [[FR-44]] → `Simulated` for that subset. Keeps PR #23
  promotions of [[FR-19]] / [[NFR-1]] / [[NFR-10]]. Per-world `simulate` is
  still scheduler-only. Maker playback is still heroes dead / Z / stalemate
  — not a live probe. [[FR-18]] C, Gunner duration, [[FR-4]] unchanged.

### Assumptions a reviewer can check

- Worlds still run to completion in FR-45 order even after a Z; the
  aggregate then marks the level a loss.
- `not_solvable` is exhaustion of the bounded candidate model, same as
  [[FR-46]]. A budget stop is unsupported, not “failed”.
- Ambiguous `dist(D, …)` with two D rooms is unsupported (no guess which
  instance).

## NFR-6 Maker wall-hatch / orientation clarity

- **Started from** `main` @ `86664b4` (honesty FR-19 / NFR-1 / NFR-10 PR #23); rebased onto `e8444c2` (PR #25 FR-43 / FR-44).
- **Producer does not merge (S7)** — PR opened for sponsor review.

### Shipped

- **Maker UI only.** Wall-hatch direction is now a wall-anchored arrow plus the four-side hatch notches on every placed room, the selected tray room, and a hover placement ghost. Compass + plain-English helper name the spawn-room ([[FR-7]]) anchor. Engine stays pure — presentation mapping lives in `apps/web/src/orientation-ui.ts`.
- **[[NFR-6]] → Simulated.** Evidence: `apps/web/src/orientation-ui.test.ts` (unique side/glyph per orientation, same words on selected + preview, helper cites FR-7). Not a live browser probe; not `Live & Probed`.
- **[[NFR-8]] → Simulated.** Evidence: `packages/engine/src/nfr8-deferrals.test.ts` — `C` stays opaque and has no invented entry effect; Gunner duration is stored and unread; campaign marks both unplayable. Does **not** close [[FR-18]], Gunner duration semantics, or [[FR-4]].
- **[[NFR-3]] stays Unknown.** Room/hero/spell dispatch is still closed `switch` / `if` chains, not a plug-in registry. No invented extension API.
- **[[NFR-7]]:** copy touched in the Maker uses the spec pair **wall-hatch direction** / **orientation**. No i18n rewrite.

See [[GAME_SPEC]], [[STATUS_LEDGER]], [[PLAYER_GUIDE]], and [[0002-typescript-monorepo-2d-to-3d]].

## Knowledge — AEA diagrams (cosmetic)

- Converted the two ASCII box drawings on the AEA knowledge page (core formula layers + 5 concentric floors) to mermaid flowcharts so they match the guide and architecture pages.
- Tuned `.mermaid-wrap` CSS so SVGs stay readable and scroll on small viewports instead of wrapping like broken `<pre>` art.
- Knowledge build still requires mermaid on the AEA page (now at least 3 diagrams; leftover ASCII box art fails the build).
- No [[STATUS_LEDGER]] promotions. Cosmetic / clarity only — AEA doctrine unchanged.

See [[AGENTS]] and [[0003-multi-agent-collaboration]].

## Knowledge — mobile / narrow viewports (cosmetic)

- Sponsor note (via ctos): `knowledge.*` pages from `apps/knowledge/` must stay
  usable on ~375px / ~480px (viewport, type, nav). Same build as guide,
  architecture, AEA, observability.
- CSS plus a `.header-tools` wrapper (nav + external links) and one AEA
  formula class (removed a fixed inline `1.25rem` so type can shrink). Under
  720px the tools row scrolls locally; cards / `.is-wide` main collapse;
  tables, `<pre>`, mermaid keep in-box overflow.
- No [[STATUS_LEDGER]] promotions. Doctrine and FR/NFR rows unchanged.

See [[AGENTS]] and [[0003-multi-agent-collaboration]].

## Knowledge — skill matrix (this PR)

- **Started from** `main` @ `2450b6c` (web mobile PR #28).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Open UX work at start: hero figurines PR #29 (draft). Kid-rules /
  persona-journey issues #30–#43 exist from the 2026-09-12 live first-play
  audit. This slice does **not** edit Maker UX or [[PLAYER_GUIDE]] kid copy
  so those can land beside it.

### Shipped

- [[SKILL_MATRIX]] (`docs/SKILL_MATRIX.md` → `skills.html`): OOTB skills
  (`code-changes`, `routines`, `box-desktop`, `skill-authoring`), shared
  Grok recipes saved 2026-09-12 (`pr-train-rebase`, `honesty-ledger-gate`,
  `companion-plain-docs`, `persona-journey-validation`), and project
  playbooks ([[AGENTS]], [[GAME_SPEC]], [[STATUS_LEDGER]], [[FINDINGS]],
  journal, ADRs). Columns: kind, when to load, why selected from this
  repo's 2026-09 history (phases 0–7, generator, AFK PR train #23/#24/#25,
  first-timer audit).
- Knowledge nav + home card. Architecture overview and AEA Procedure
  Memory point at the page. Build fails if the matrix file drops a named
  skill or its mermaid / table.
- No [[STATUS_LEDGER]] promotions. Does not invent `C`, Gunner duration,
  or [[FR-4]] gating.

See [[AGENTS]], [[0003-multi-agent-collaboration]], and [[SKILL_MATRIX]].

## AEA — keep learning and apply (this PR)

- **Composed on** skill-matrix branch `cursor/skill-matrix-551b` (PR #49,
  bc-53cc542e) rather than duplicating the matrix. Rebase onto that PR
  if it moves.
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Tracks [#53](https://github.com/artofdream/zorg-dungeon/issues/53).
  Links shared skills and [#50](https://github.com/artofdream/zorg-dungeon/issues/50)–[#52](https://github.com/artofdream/zorg-dungeon/issues/52).
- Does **not** edit Maker UX or the in-flight persona-journeys / kid learn
  page (journeys PR #44; `docs/PLAYER_JOURNEYS.md` is not on this branch).
  Those gaps stay follow-ups, not this diff.
- GitHub `issues: write` returned 403 from this agent token, so
  [#53](https://github.com/artofdream/zorg-dungeon/issues/53) could not
  be patched here. Replacement body (official #434 wording) is in PR
  #54 for a reviewer to paste. Journeys/learn-page gap stays drafted
  there too. Formal AEA ID is now cited in-repo (work item #434).

### Shipped (docs only)

- [[AGENTS]]: **Keep Learning and Apply**
  ([AEA #434](https://gitlab.com/artof-group/adaptive-experience-architecture/-/work_items/434)
  / open [!513](https://gitlab.com/artof-group/adaptive-experience-architecture/-/merge_requests/513))
  next to Honesty and Knowledge First. Official wording: write the
  lesson into the harness before the next loop. Complements Honesty +
  Knowledge First; **not** Antifragility (§3). AEA skill matrix:
  merged [!512](https://gitlab.com/artof-group/adaptive-experience-architecture/-/merge_requests/512)
  (closes [#433](https://gitlab.com/artof-group/adaptive-experience-architecture/-/work_items/433)),
  file `research/random-thoughts/2026-09-12-session-memory-log-aea-grok-skill-matrix.md`.
  Do not treat !513 as merged. GitLab pages redirected to sign-in from
  this agent — not a content probe.
- [[SKILL_MATRIX]]: section + table mapping each skill as an
  apply-artifact of historical pain. Planned candidates for #51 / #52.
  Status **Documented / Planned**. Knowledge Pages: **Unknown** until
  AEA probes.
- Knowledge `aea.html` + home Skills card cite #434. Build fails if
  the matrix or AEA page drops the name or the work-item URL.
- [[FINDINGS]] CF-010 (skills written only after AFK rebase pain) —
  Open, Recurrence 1. CF-009 left for journeys PR #44.

### Honesty

- No [[STATUS_LEDGER]] promotions. No invented `C`, Gunner duration, or
  [[FR-4]] gating. Naming a principle is not a production probe.

See [[AGENTS]], [[SKILL_MATRIX]], [[FINDINGS]] CF-010, and
[[0003-multi-agent-collaboration]].

## Web Maker — mobile / narrow viewports (cosmetic)

- Sponsor: `zorg.artof.link` (`apps/web` — campaign + Maker + extermination)
  should stay usable at ~375px / ~480px. Viewport meta was already present.
  This is layout / tap-target / overflow work, not new game rules.
- CSS tokens (`--cell`, `--tap`) plus light classes (`section-label`,
  `toggle`, `play-title`, panel roles). 3-column Maker still collapses at
  900px; phones get 56px then 48px cells (scroll the board, no pinch),
  44px primary controls, wrapping filters, honesty/lede wrap, no page-wide
  horizontal scroll. Cell inner markup left alone so a parallel hero-icon
  PR can share `.cell` / `--cell`.
- No [[STATUS_LEDGER]] promotions (cosmetics). No C / Gunner duration /
  [[FR-4]] behavior.

See [[AGENTS]], [[0002-typescript-monorepo-2d-to-3d]], and [[NFR-6]].

## Web — hero figurines (cosmetic)

- **Started from** `main` @ `ac3d0ff` (knowledge mobile-responsive PR #27); rebased onto `main` @ `6d108ec` (Keep Learning and Apply PR #54) so this sits on the mobile `--cell` Maker plus the docs slice.
- **Producer does not merge (S7)** — PR opened for review.

### Shipped

- Hand-authored 32×32 SVGs for every engine `HeroType` (Warrior, Elf, Gunner, Mechanic, Princess) under `apps/web/public/heroes/`. Same dark / gold / accent palette as the favicon. Style reference only — not a raster sheet and not a rules change.
- Maker roster, choix hero picks, `.cell.hero` occupants, extermination playback, and campaign cards show the figurines next to existing text labels. Optional Maker legend names the five types. Cell icons scale with `--cell` so the mobile Maker (PR #28) stays usable.
- Engine stays pure. No [[FR-18]] `C` room, no Gunner duration, no [[STATUS_LEDGER]] promotions. Evidence that the assets exist and map 1:1 is `apps/web/src/hero-icons.test.ts` (UI mapping only — not a live probe).

See [[GAME_SPEC]], [[PLAYER_GUIDE]], and [[0002-typescript-monorepo-2d-to-3d]].

## Persona journeys + first-timer landing

- **Rebased onto** `main` after mobile #28, docs #54/#49, and hero figurines #29.
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Keeps #29 hooks: `HeroTypeRow` on campaign cards, `HeroIcon` on the
  Maker board / roster. Journeys copy does not replace those hunks.

### Shipped

- [[PLAYER_JOURNEYS]] — five personas used as UX validation cases
  (J-KID, J-HELPER, J-CAMPAIGN, J-PRACTICE, J-HONESTY). Happy path,
  success criteria, manual checklist, optional future automated probes.
  [[GAME_SPEC]] still wins on rules. Knowledge page `journeys.html`.
- [[LEARN]] — kid rules page (`learn.html`, nav **Learn the rules**).
  Short sentences + mermaid pictures (A → rooms → Z, two jobs, win/lose).
  Implemented rooms / heroes / spells only. FR ids live in a grown-up
  note. Linked from the Maker How to play panel.
- Maker landing: kid-facing How to play, helper one-liner, **Learn the
  rules** + guide + journeys links, **Start here — Difficulty 1** and
  **Start here — Generate Difficulty 1**. **Difficulty** is the English
  word; Difficulté stays as the source / bilingual hint. FR / C / Gunner /
  ledger honesty sits in a `<details>` disclosure (J-HONESTY).
- Maker play: Start fight label, A-toward-Z reminder, softer gate/outcome
  words; honesty still available in a disclosure.
- [[FINDINGS]] CF-009 (first-timer landing was engineer-first) →
  Resolved. Sensor: `apps/web/src/first-run.test.ts`. Recurrence 1.
- Follow-up GitHub issues for leftovers that this PR does not fix:
  [#46](https://github.com/artofdream/zorg-dungeon/issues/46) automated
  journey probes, [#47](https://github.com/artofdream/zorg-dungeon/issues/47)
  kid-readable board names, [#48](https://github.com/artofdream/zorg-dungeon/issues/48)
  Maker-play engineer jargon. (#45 is a stub create — use #48.)
  Labels `ux` / `journeys` could not be created (403); titles carry
  `[ux][journeys]` and issues use `enhancement` + `accessibility` /
  `documentation`.

### Honesty

- No [[STATUS_LEDGER]] promotions. No invented `C`, Gunner duration, or
  [[FR-4]] earn/spend. Generated play remains Simulated engine output.

See [[LEARN]], [[PLAYER_GUIDE]], [[PLAYER_JOURNEYS]], [[FINDINGS]] CF-009, and
[[0005-level-generator]].

