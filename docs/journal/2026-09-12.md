# 2026-09-12 — Difficulté level generator

- **Started from** `main` @ `11032fd` (knowledge companion refresh PR #20).
- **Producer does not merge (S7)** — PR opened for sponsor review.

## Shipped

- **`generateLevel` in `@zorg/engine`**: seed-stable, parseable DSL, legal
  Maker line layout ([[FR-5]]–[[FR-8]]), intended [[FR-46]] solvable within
  [[NFR-4]] caps. Evidence: `packages/engine/src/generator.ts`,
  `packages/engine/src/generator.test.ts`. See [[0005-level-generator]].
- Difficulté model is the authored numeric bands **1–4**, with knobs checked
  against playable fixture envelopes (not a 1–10 fantasy scale). Harder bands
  add rooms / a second hero / spells / E+O flavour. Band 4 matches the only
  authored Difficulté 4 board (9 rooms). Band 3 does not jump the board size
  vs band 2 — that matches the corpus.
- Construction guarantee: A, then a lethal `D`, then flavour rooms, then Z.
  First D damage ≥ every hero's HP. No mirrors (no intentional not_solvable
  worlds in this slice).
- **Does not invent:** `C` ([[FR-18]] / S2), Gunner duration (S3 — Gunner is
  not generated), [[FR-4]] earn/spend (S6), contracts 11–15 (S4), quarantine
  N11/N18 (CF-005). Mechanic / Princess / P / T skipped so the corridor proof
  stays a construction, not a hope.
- **Web:** campaign browser stays the authored default. A Generate panel
  (Difficulté 1–4) and Maker **Regenerate** are additive. Honesty copy:
  generated = Simulated engine, not Live & Probed; FR-4 still not gating.
- **Docs:** [[0005-level-generator]], this journal, [[PLAYER_GUIDE]] note.
  [[STATUS_LEDGER]] rows are **not** promoted. [[FINDINGS]] unchanged — no new
  CF. [[GAME_SPEC]] FRs not rewritten.

## Assumptions a reviewer can check

- Omitted `seed` is `1`, not `Date.now()`.
- Suggested layout is orientation 0, A-first / Z-last, default 4-hatch tiles.
- `checkSolvability` on that layout is Simulated proof, not a live probe.
- Generated campaign cards have `pack: "generated"` and no contract cost.

See [[GAME_SPEC]], [[STATUS_LEDGER]], [[PLAYER_GUIDE]], and [[0005-level-generator]].

## Ops — CF-006 closed

- [[FINDINGS]] CF-006 (production web image had no CD and went stale) is **Resolved**.
- CD privilege fix: [PR #19](https://github.com/artofdream/zorg-dungeon/pull/19) (`sudo git` on Lightsail `/opt/zorg`).
- Post-merge evidence: `deploy-web` on the [PR #21](https://github.com/artofdream/zorg-dungeon/pull/21) merge to `main` (`4111d82`) succeeded — Actions run [34687483872](https://github.com/artofdream/zorg-dungeon/actions/runs/34687483872).
- **Ops only.** Green CD is not a production content probe — [[STATUS_LEDGER]] FR/NFR rows are unchanged.

See [[FINDINGS]] CF-006 and [[0004-deployment-and-observability]].

## Knowledge — AEA diagrams (cosmetic)

- Converted the two ASCII box drawings on the AEA knowledge page (core formula layers + 5 concentric floors) to mermaid flowcharts so they match the guide and architecture pages.
- Tuned `.mermaid-wrap` CSS so SVGs stay readable and scroll on small viewports instead of wrapping like broken `<pre>` art.
- Knowledge build still requires mermaid on the AEA page (now at least 3 diagrams; leftover ASCII box art fails the build).
- No [[STATUS_LEDGER]] promotions. Cosmetic / clarity only — AEA doctrine unchanged.

See [[AGENTS]] and [[0003-multi-agent-collaboration]].
