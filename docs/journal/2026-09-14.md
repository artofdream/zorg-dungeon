# 2026-09-14 — UX laptop + mobile gaps (Maker + knowledge)

- **Started from** `main` @ `d1fb25b` (i18n PR #62).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — Shell has no `machineId`; preferred EVO-X2
  `f074e48c-ff1d-4a00-945c-74838a5b1550` at `C:\projects\code\zorg-dungeon`
  unavailable to this executor. Clone + work on the shared Grok box.

## Shipped

Laptop + mobile UX gap pass on Maker (`apps/web`) and knowledge
(`apps/knowledge`). CSS/HTML only plus small sensors — no game-rule inventing,
no Phase C honesty/skills i18n expansion beyond existing `lang` attributes.

### Fixed

1. **Knowledge viewport** — `viewport-fit=cover` on HTML shell (was
   device-width + initial-scale only).
2. **Tap targets** — locale switcher EN|FR ≥ `--tap` / 2.75rem on ≤480px
   (Maker buttons + knowledge links); primary CTAs / filters already on
   `--tap` at narrow breakpoints.
3. **Focus-visible** — knowledge gains `:focus-visible` rings; Maker already
   had them; no bare `outline: none`.
4. **Locale switcher overflow** — `flex-shrink: 0`, larger hit area, sits in
   existing scrollable `.header-tools` on knowledge narrow.
5. **Safe-area** — knowledge header/body/footer padding uses
   `env(safe-area-inset-*)`; Maker `.app` already did.
6. **Skip-to-content** — Maker `#main-content` + i18n `chrome.skipToContent`;
   knowledge `#content` + EN/FR skip link in `pageShell`.
7. **`lang="fr"`** — already set by build for FR companions; build now
   asserts it (plus viewport-fit + skip) on `fr/*.html`.
8. **`prefers-reduced-motion`** — both stylesheets; knowledge card hover lift
   disabled under reduce.
9. **Touch-action** — knowledge nav / external / locale links get
   `manipulation` (Maker mostly already had it).
10. **Laptop measure** — knowledge prose (`p` / `.lead` / learn|guide|journeys)
    capped at `72ch`; Maker lede/honesty already `70ch`.

### Sensors

- Knowledge build assertions: viewport-fit, skip link, `#content`, FR
  `lang="fr"`.
- Vitest `apps/web/src/ux-smoke.test.ts`: viewport meta, focus-visible, no
  bare outline:none, reduced-motion, locale `--tap`.

### Deferred

- Live visual probe at 390 / 768 / 1280 of FR nav EN↔FR (tables/mermaid wrap
  already present; no code change beyond CSS). Simulated CSS + build sensors
  only here.
- Sticky Maker `.topbar` height vs board — already sticky with board-wrap
  scroll; no change.
- Phase C honesty/skills/spec bilingual; Room `C` / Gunner duration / [[FR-4]].

## Honesty

- **Simulated** CSS/HTML + unit/build sensors. Not Live & Probed. No
  [[STATUS_LEDGER]] promotion. Does not invent `C`, Gunner duration, or
  [[FR-4]].

See [[0007-i18n-fr-en]], [[PLAYER_JOURNEYS]], [[AGENTS]].
