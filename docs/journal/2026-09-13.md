# 2026-09-13 — Journey browser probes (#46)

- **Started from** `main` @ `7109bb3` (kid UX PR #57).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — EVO-X2 `machineId` Shell unavailable to this
  executor; clone + probes on the shared Grok box (same pattern as prior
  kid-UX work).

## Shipped

- Playwright suite `apps/web/e2e/journeys.spec.ts` probes the persona
  journeys in [[PLAYER_JOURNEYS]] / [#46](https://github.com/artofdream/zorg-dungeon/issues/46):
  landing How to play + both Start here labels; kid-facing blob free of
  FR-4 / C room / Gunner duration / ledger; Start here Difficulty 1 →
  authored band 1; Start here Generate → `pack === generated` band 1;
  authored catalog default + unfinished toggle off; honesty disclosure
  still names Simulated / FR-4 / C / Gunner. Optional: Maker title +
  Place rooms in a line / Start fight enable after legal layout.
- Labels reuse `apps/web/src/first-run.ts` strings. Maker exposes `data-pack` /
  `data-difficulty-band` for honest pack/band asserts (presentation only).
- CI wiring: intended `ci.yml` steps are in the PR (Install Playwright
  Chromium + Journey probes). This agent’s OAuth token lacks the GitHub
  `workflow` scope, so the workflow file was **not** pushed — sponsor can
  paste the two steps. Local: `pnpm probe:journeys` (builds, then probes).
- Light cite: Keep Learning and Apply
  ([AEA #434](https://gitlab.com/artof-group/adaptive-experience-architecture/-/work_items/434))
  — write the journey checklist into a browser sensor before the next
  first-play loop.

## Honesty

- **Simulated** browser evidence only. Not Live & Probed. Not a
  [[STATUS_LEDGER]] promotion. Does not invent `C`, Gunner duration, or
  [[FR-4]] gating.

See [[PLAYER_JOURNEYS]], [[FINDINGS]] CF-009, and [[AGENTS]].

## Web — illustrated Maker room tiles (#37)

- **Started from** `main` @ journey-probes merge (PR #58 / #46 on main).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — Shell has no `machineId`; work + push via `gh`
  as `artofdream` on the shared Grok box (EVO-X2 preferred path unavailable).

### Shipped

- Kid-friendly SVG room tiles under `apps/web/public/rooms/` for
  **A / Z / D / E / P / O / T** (door/start, exit, danger, element, portal,
  gold, toll) matching hero figurine warm palette + stroke style.
- Maker board cells show the tile illustration + kid word; raw coordinates
  stay in `aria-label` / `data-coords` and a de-emphasized empty-cell meta.
- Empty cells orthogonally adjacent to placed rooms get `.legal-next`
  highlight while a tray room is selected (placement hint only — does not
  change engine legality / [[FR-5]]–[[FR-8]] validation).
- Modest `--cell` upsize (72 / 64 / 56) so tiles read near ~390px width;
  hero icons remain overlaid. Tray buttons show a small tile thumb.
- **No C tile** — C stays label-only (`Special`); no FR-4 / Gunner duration
  invented. App.tsx has no separate board (MakerPlay only).

### Evidence

- Unit: `apps/web/src/room-icons.test.ts` (assets + legal-next helper).
- Intended: `pnpm test` / `typecheck` / `governance` / `build` /
  `probe:journeys` on the PR branch.

See [#37](https://github.com/artofdream/zorg-dungeon/issues/37).

## Findings — close CF-005 + CF-010 (quarantine sensor / Keep Learning probe)

- **Started from** `main` @ `40dd049` (illustrated tiles PR #60).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — Shell has no `machineId`; preferred EVO-X2
  `f074e48c-ff1d-4a00-945c-74838a5b1550` at `C:\projects\code\zorg-dungeon`
  unavailable to this executor. Clone + work on the shared Grok box.

### Shipped

- **CF-005 → Resolved.** Named sensor
  `packages/engine/src/quarantine-index.test.ts` asserts
  `fixtures/quarantine-index.json` paths exist, green globs exclude them,
  expected ids (base-classic 11/18/23, deluxe 8.2 / 10.7). Does not invent
  Dream Trap author fixes. Recurrence stays 1. Cite:
  `corpus-quarantine-honesty` / Keep Learning and Apply lightly.
- **CF-010 → Resolved.** Sensors: [[AGENTS]] Keep Learning section +
  [[SKILL_MATRIX]] + knowledge skills page build. Apply evidence: post-finding
  PRs (#58 / #59 / #60). **zorg knowledge Pages Probed** this session
  (HTTP 200): skills.html, aea.html, findings.html on
  knowledge.zorg.artof.link. **AEA Pages stay Unknown.** Not Live & Probed
  for game FRs; no [[STATUS_LEDGER]] promotion.

### Honesty

- No inventing `C` / Gunner duration / [[FR-4]]. No FR row promotions.

## i18n FR/EN — Maker + knowledge companions (ADR-0007)

- **Started from** `main` @ `13d971b`.
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — Shell has no `machineId`; preferred EVO-X2
  `f074e48c-ff1d-4a00-945c-74838a5b1550` at `C:\projects\code\zorg-dungeon`
  unavailable to this executor. Work on the shared Grok box clone.

### Shipped

- [[0007-i18n-fr-en]]: message catalogs; display-only translation; GAME_SPEC
  English IDs win; URL strategy `?lang=fr` (+ `document.documentElement.lang`
  + localStorage); EN keys SoT; FR complete catalog; CI missing-key gate.
- Phase A (`apps/web`): `locales/en.ts` + `fr.ts`, tiny `t()` + LocaleProvider,
  EN|FR switcher on landing/Maker chrome; first-run / event-copy / kid labels
  read catalogs; Learn-the-rules href follows locale → `fr/learn.html`.
- Phase B (`docs/fr/` + knowledge build): FR LEARN / PLAYER_GUIDE /
  PLAYER_JOURNEYS → `fr/learn.html`, `fr/guide.html`, `fr/journeys.html` with
  EN↔FR nav. Same FR/NFR IDs; French prose; spec wins callout.
- Tests: catalog coverage both ways; first-run / event-copy FR cases; journey
  FR smoke `?lang=fr`. [[PLAYER_JOURNEYS]] notes optional FR validation.

### Explicitly deferred

- Phase C: honesty.html / skills.html / full GAME_SPEC bilingual.
- Path-prefix `/fr/` Maker routing (no React Router this slice).
- Room `C`, Gunner duration, [[FR-4]] — still not invented ([[NFR-8]]).

### Honesty

- No [[STATUS_LEDGER]] FR promotions. Journey / catalog evidence is Simulated
  only. No inventing C / Gunner duration / FR-4.
