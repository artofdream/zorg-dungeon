# 2026-09-13 — Journey browser probes (#46)

- **Started from** `main` @ `7109bb3` (kid UX PR #57).
- **Producer does not merge (S7)** — PR opened for sponsor review.
- Machine: **(Grok box)** — EVO-X2 `machineId` Shell unavailable to this
  executor; clone + probes on the shared Grok box (same pattern as prior
  kid-UX work).

## Shipped

- Playwright suite `apps/web/e2e/journeys.spec.ts` probes the persona
  journeys in [[PLAYER_JOURNEYS]] / [#46](https://github.com/artofdream/zorg-dungeon/issues/46):
  landing How to play + both Start here labels; kid-facing blob free of
  FR-4 / C room / Gunner duration / ledger; Start here Difficulty 1 →
  authored band 1; Start here Generate → `pack === generated` band 1;
  authored catalog default + unfinished toggle off; honesty disclosure
  still names Simulated / FR-4 / C / Gunner. Optional: Maker title +
  Place rooms in a line / Start fight enable after legal layout.
- Labels reuse `apps/web/src/first-run.ts` strings. Maker exposes `data-pack` /
  `data-difficulty-band` for honest pack/band asserts (presentation only).
- CI wiring: intended `ci.yml` steps are in the PR (Install Playwright
  Chromium + Journey probes). This agent’s OAuth token lacks the GitHub
  `workflow` scope, so the workflow file was **not** pushed — sponsor can
  paste the two steps. Local: `pnpm probe:journeys` (builds, then probes).
- Light cite: Keep Learning and Apply
  ([AEA #434](https://gitlab.com/artof-group/adaptive-experience-architecture/-/work_items/434))
  — write the journey checklist into a browser sensor before the next
  first-play loop.

## Honesty

- **Simulated** browser evidence only. Not Live & Probed. Not a
  [[STATUS_LEDGER]] promotion. Does not invent `C`, Gunner duration, or
  [[FR-4]] gating.

See [[PLAYER_JOURNEYS]], [[FINDINGS]] CF-009, and [[AGENTS]].
